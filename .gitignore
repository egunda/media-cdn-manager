# Credentials and keys
credentials/
key.json
*.json
!package.json
!dual-token-vod.json
!dual-token-vod-clone.json

# If you want to keep the specific dual-token files as examples, we can uncomment them.
# But generally, it's safer to ignore JSON files if they might contain project IDs.
# Let's be explicit.
credentials/
key.json
project_info.txt

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/

# Environment/Project specific
server.log
.DS_Store

# UI/Build
node_modules/
.env
